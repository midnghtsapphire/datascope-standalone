{
  "name": "datascope-standalone-revvel-baseline",
  "private": true,
  "version": "1.0.0",
  "description": "Revvel-standards baseline manifest for DataScope Standalone",
  "scripts": {
    "test": "node scripts/test-baseline.js",
    "build": "node scripts/build-baseline.js"
  }
}
